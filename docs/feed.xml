<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://theroboticsclub.github.io/gsoc2026-Ashwani_Kumar/feed.xml" rel="self" type="application/atom+xml"/><link href="https://theroboticsclub.github.io/gsoc2026-Ashwani_Kumar/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-29T18:48:55+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Ashwani_Kumar/feed.xml</id><title type="html">Ashwani Kumar Moudgil: GSoC X JdeRobot 2026</title><subtitle>GSoC 2026 blog — work, weekly updates, and notes from my project on RoboticsAcademy @ JdeRobot. </subtitle><entry><title type="html">Week 2 — One click, one new universe</title><link href="https://theroboticsclub.github.io/gsoc2026-Ashwani_Kumar/blog/2026/week2-palletizing-universe/" rel="alternate" type="text/html" title="Week 2 — One click, one new universe"/><published>2026-05-28T00:00:00+00:00</published><updated>2026-05-28T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Ashwani_Kumar/blog/2026/week2-palletizing-universe</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Ashwani_Kumar/blog/2026/week2-palletizing-universe/"><![CDATA[<blockquote> <p><em>GSoC 2026 · Week 2 · Coding period begins</em></p> </blockquote> <h1 id="week-2-new-palletizing-universe">Week 2: New Palletizing Universe</h1> <p>Last week I spent staring at code paths. This week I spent builing something.</p> <p>The brief: stand up a new <strong>Palletizing Harmonic</strong> universe inside RoboticsAcademy, reusing the UR5 + Robotiq 85 gripper from Pick &amp; Place, but with a different scene — six identical boxes on the conveyor, a pallet platform waiting to be stacked. The fun question wasn’t “can I do it?” but “<em>how little do I have to touch?</em>” RoboticsAcademy spans four repos and four runtime stages, and there’s a temptation, when adding a feature, to reach into all of them. The discipline this week was resisting that.</p> <p>The answer turned out to be reassuringly small. <strong>One SDF world, two launch files, three SQL rows, and one tiny patch</strong> — and the dropdown lights up with a new entry that launches end-to-end.</p> <h2 id="the-path-of-one-click">The path of one click</h2> <p>Before the diff, the picture. Here’s everything that happens between <em>click</em> and <em>Gazebo window</em>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>   SQL seed files
        │
        ▼
   Postgres (universe_db)
        │     SELECT ... JOIN exercises_universes JOIN universes JOIN worlds
        ▼
   Django backend  ──── HTTP :7164 ────►  React UI
        │                                    │
        │   ◄──── WebSocket :7163 ───────────┘   "launch this universe"
        ▼
   Robotics Application Manager (RAM)
        │   FSM: idle → connected → world_ready → tools_ready → running
        ▼
   subprocess.Popen("ros2 launch palletizing_harmonic.launch.py")
        │
        ▼
   gz sim · MoveIt · ros_gz_bridge · controllers · RViz (separately)
</code></pre></div></div> <p>Adding a universe means stitching a new row through stage 1, and dropping a new launch file at the end of stage 4. Every stage in between just relays. <em>That’s</em> what makes the change so small — the architecture does the heavy lifting for you, as long as you don’t fight it.</p> <h2 id="the-diff-in-pieces">The diff, in pieces</h2> <h3 id="1-the-world-palletizing_arm_harmonicworld">1. The world (<code class="language-plaintext highlighter-rouge">palletizing_arm_harmonic.world</code>)</h3> <p>A 317-line SDF file at <code class="language-plaintext highlighter-rouge">Industrial/robotiq_description/world/</code>. About 250 lines of it are copied verbatim from <code class="language-plaintext highlighter-rouge">warehouse_arm_harmonic.world</code> — the ground plane, lighting, conveyor, target table (which doubles as the pallet platform for now), the robot base cylinder, and the obligatory <code class="language-plaintext highlighter-rouge">gz-sim-physics-system</code> / <code class="language-plaintext highlighter-rouge">SceneBroadcaster</code> / <code class="language-plaintext highlighter-rouge">LinkAttacher</code> plugins. The only real authoring was the <strong>pickable section</strong>: six identical brown boxes laid out in a 2×3 grid on the conveyor side, sitting at <code class="language-plaintext highlighter-rouge">z = 1.04</code> so they hover just above the conveyor surface.</p> <p>The discipline here is <em>don’t move what isn’t broken</em>. The arm doesn’t shift. The conveyor doesn’t shift. The robot base stays at the same pose. That keeps every MoveIt frame, every controller config, every TF tree from Pick &amp; Place valid — and means I don’t have to retune a single planner parameter.</p> <h3 id="2-the-two-launchers">2. The two launchers</h3> <p>RAM expects every Harmonic universe to ship a pair: a <strong>main launcher</strong> that brings up <code class="language-plaintext highlighter-rouge">gz sim</code>, the arm, controllers, and the gz↔ROS bridge; and a <strong>separate RViz launcher</strong> that RAM kicks off as its own process. They communicate only over ROS topics — neither knows the other exists.</p> <p>The main launcher (<code class="language-plaintext highlighter-rouge">Launchers/palletizing_harmonic.launch.py</code>) is <em>73 lines and almost no logic</em>. It sets <code class="language-plaintext highlighter-rouge">GZ_SIM_RESOURCE_PATH</code> so gz can find the meshes, then includes the same <code class="language-plaintext highlighter-rouge">spawn_robot_warehouse.launch.py</code> that Pick &amp; Place uses — only overriding the world path:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">warehouse_launch</span> <span class="o">=</span> <span class="nc">IncludeLaunchDescription</span><span class="p">(</span>
    <span class="nc">PythonLaunchDescriptionSource</span><span class="p">(</span><span class="n">warehouse_launch_file</span><span class="p">),</span>
    <span class="n">launch_arguments</span><span class="o">=</span><span class="p">{</span>
        <span class="sh">"</span><span class="s">launch_rviz</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">false</span><span class="sh">"</span><span class="p">,</span>          <span class="c1"># Academy launches RViz separately
</span>        <span class="sh">"</span><span class="s">world_file</span><span class="sh">"</span><span class="p">:</span> <span class="n">palletizing_world_file</span><span class="p">,</span>
    <span class="p">}.</span><span class="nf">items</span><span class="p">(),</span>
<span class="p">)</span>
</code></pre></div></div> <p>The RViz companion (<code class="language-plaintext highlighter-rouge">Launchers/rviz/palletizing_harmonic.launch.py</code>) is a near-verbatim clone of its Pick &amp; Place sibling, with only the docstring header changed. Same arm xacro, same MoveIt config, same RViz layout. Genuinely a one-character-class-of-edit file.</p> <h3 id="3-the-patch-that-makes-both-launchers-possible">3. The patch that makes both launchers possible</h3> <p>The piece I’m proudest of, because it’s small but it <em>unlocks</em> the rest. The Pick &amp; Place stack inherits a launch file from <code class="language-plaintext highlighter-rouge">ur5_gripper_description</code> called <code class="language-plaintext highlighter-rouge">spawn_robot_warehouse.launch.py</code>, which composes the full simulator. Until this week, the world path was hardcoded inside it:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">world_file</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span>
    <span class="n">robotiq_pkg_share_dir</span><span class="p">,</span> <span class="sh">"</span><span class="s">world</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">warehouse_arm_harmonic.world</span><span class="sh">"</span>
<span class="p">)</span>
</code></pre></div></div> <p>That hardcoding is what makes “just add a world” <em>not</em> work — every new world had to bring its own full copy of the composition. I lifted the path into a launch argument with a backwards-compatible default:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">default_world_file</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span>
    <span class="n">robotiq_pkg_share_dir</span><span class="p">,</span> <span class="sh">"</span><span class="s">world</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">warehouse_arm_harmonic.world</span><span class="sh">"</span>
<span class="p">)</span>

<span class="n">declared_arguments</span> <span class="o">=</span> <span class="p">[</span>
    <span class="nc">DeclareLaunchArgument</span><span class="p">(</span><span class="sh">"</span><span class="s">ur_type</span><span class="sh">"</span><span class="p">,</span> <span class="n">default_value</span><span class="o">=</span><span class="sh">"</span><span class="s">ur5</span><span class="sh">"</span><span class="p">),</span>
    <span class="nc">DeclareLaunchArgument</span><span class="p">(</span><span class="sh">"</span><span class="s">launch_rviz</span><span class="sh">"</span><span class="p">,</span> <span class="n">default_value</span><span class="o">=</span><span class="sh">"</span><span class="s">true</span><span class="sh">"</span><span class="p">),</span>
    <span class="nc">DeclareLaunchArgument</span><span class="p">(</span>
        <span class="sh">"</span><span class="s">world_file</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">default_value</span><span class="o">=</span><span class="n">default_world_file</span><span class="p">,</span>
        <span class="n">description</span><span class="o">=</span><span class="sh">"</span><span class="s">Absolute path to the .world file to load in gz sim</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">),</span>
<span class="p">]</span>
<span class="c1"># ...
</span><span class="n">world_file</span> <span class="o">=</span> <span class="nc">LaunchConfiguration</span><span class="p">(</span><span class="sh">"</span><span class="s">world_file</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>Pick &amp; Place doesn’t notice — its call site passes no <code class="language-plaintext highlighter-rouge">world_file</code>, so the default kicks in. Palletizing passes its own. Any <em>future</em> universe that reuses this arm gets the same shortcut for free.</p> <p>This is the change I want to land upstream first, because everything else depends on it.</p> <h3 id="4-three-sql-rows">4. Three SQL rows</h3> <p>The database is the catalog. Three small rows are enough:</p> <table> <thead> <tr> <th>Where</th> <th>Row</th> <th>Meaning</th> </tr> </thead> <tbody> <tr> <td>RoboticsInfrastructure <code class="language-plaintext highlighter-rouge">database</code> branch — <code class="language-plaintext highlighter-rouge">universes.sql</code></td> <td><code class="language-plaintext highlighter-rouge">worlds (id=68, type=gz, launch_file_path=/opt/jderobot/Launchers/palletizing_harmonic.launch.py, tools_config={"rviz":".../rviz/palletizing_harmonic.launch.py"})</code></td> <td>“Here’s a world.”</td> </tr> <tr> <td>same file</td> <td><code class="language-plaintext highlighter-rouge">universes (id=68, name='Palletizing Harmonic World', world_id=68, robot_id=0)</code></td> <td>“It’s a launchable universe.”</td> </tr> <tr> <td>RoboticsAcademy — <code class="language-plaintext highlighter-rouge">database/exercises/db.sql</code></td> <td><code class="language-plaintext highlighter-rouge">exercises_universes (71, 17, 68, False)</code></td> <td>“It belongs inside exercise 17 (Pick &amp; Place), and it isn’t the default.”</td> </tr> </tbody> </table> <p>That third row is the one that makes the new universe show up in the dropdown <em>without</em> requiring a new exercise card, new icon, new React route, or any frontend change at all. The UI is rendered straight from the JSON Django assembles by joining these tables. Add a row, restart with <code class="language-plaintext highlighter-rouge">down -v</code>, refresh — there it is.</p> <h2 id="smoke-test">Smoke test</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dropdown: "Palletizing Harmonic World"  ─► click ─► play
    │
    ▼
RAM logs:  idle → connected → world_ready → tools_ready → application_running
    │
    ▼
noVNC at :6080 shows gz sim window
    Scene: ground · conveyor · table · UR5 + Robotiq 85 · 6 brown boxes
RViz comes up in a second window. MoveIt planning groups loaded.
</code></pre></div></div> <p>It works.</p> <h2 id="things-that-nearly-bit-me">Things that nearly bit me</h2> <ul> <li><strong>The SQL columns are tab-separated.</strong> Postgres <code class="language-plaintext highlighter-rouge">COPY ... FROM stdin;</code> is strict. Any editor that helpfully expands tabs into spaces silently breaks the seed and you spend twenty minutes wondering why your row “isn’t being inserted.”</li> <li><strong>The world’s root element must be <code class="language-plaintext highlighter-rouge">&lt;world name="default"&gt;</code>.</strong> Renaming it to <code class="language-plaintext highlighter-rouge">"palletizing"</code> (which I tried, because it felt cleaner) breaks the bridge naming downstream. Fight that instinct.</li> <li><strong>Colcon installs the world to two places.</strong> The source <code class="language-plaintext highlighter-rouge">world/</code> directory and the install tree at <code class="language-plaintext highlighter-rouge">install/robotiq_description/share/robotiq_description/world/</code>. During iteration you either edit both or rebuild the package. I lost half an hour to this before noticing.</li> <li><strong>The Postgres volume is anonymous on purpose.</strong> <code class="language-plaintext highlighter-rouge">docker compose down -v</code> re-seeds from the SQL files. Forgetting the <code class="language-plaintext highlighter-rouge">-v</code> flag means your changes are invisible. Don’t trust the cached database.</li> </ul> <h2 id="the-bigger-lesson">The bigger lesson</h2> <p>What I keep coming back to is how <em>much</em> of this week’s work was reading rather than writing. The actual diff is maybe 400 lines, most of it copy-pasted SDF. But the design — the call to <em>not</em> add a new exercise, <em>not</em> add a new robot, <em>not</em> fork the composition launcher, <em>not</em> hardcode a second world path — required understanding the four-stage pipeline well enough to know which seams to cut along.</p> <p>I wrote those mental models down as I learned them — eight numbered chapters now sitting at <a href="https://github.com/JdeRobot/RoboticsAcademy/tree/humble-devel/learning-notes/universe-launch"><code class="language-plaintext highlighter-rouge">learning-notes/universe-launch/</code></a>. They were originally for me; if any of them turn out to be useful for the next person doing this, I’ll polish them into standalone posts.</p> <h2 id="loose-ends">Loose ends</h2> <p>The arm doesn’t <em>do</em> anything yet once the scene loads — there’s no palletizing motion plan written. That’s next week. The pallet platform is the target table reused as-is; a proper slatted pallet model would look more convincing, but it’s cosmetic.</p> <h2 id="whats-next">What’s next</h2> <ul> <li>A real palletizing motion plan — pick a box, place it on the pallet, repeat for the 2×3 grid. The point is to confirm that MoveIt carries over from Pick &amp; Place unmodified.</li> <li>Open the upstream PRs: three new files to RoboticsInfrastructure <code class="language-plaintext highlighter-rouge">humble-devel</code>, two SQL rows to the <code class="language-plaintext highlighter-rouge">database</code> branch, one to RoboticsAcademy <code class="language-plaintext highlighter-rouge">humble-devel</code>.</li> <li>Record a short demo video for the next post — the conveyor and the boxes are photogenic in a way that screenshots don’t quite capture.</li> <li>Possibly: a standalone post on <em>why RAM exists at all</em> (why Django doesn’t just <code class="language-plaintext highlighter-rouge">subprocess.Popen</code> directly). I drafted it as a learning note this week and I think the argument is worth its own page.</li> </ul> <p>Until next week 👋</p>]]></content><author><name></name></author><category term="updates"/><category term="gsoc"/><category term="roboticsacademy"/><category term="gazebo-harmonic"/><category term="ros2"/><summary type="html"><![CDATA[Following a single click from the React UI all the way to `ros2 launch` — and slipping a brand-new Palletizing Harmonic world into that pipeline along the way.]]></summary></entry><entry><title type="html">Hello, GSoC 2026</title><link href="https://theroboticsclub.github.io/gsoc2026-Ashwani_Kumar/blog/2026/hello-gsoc/" rel="alternate" type="text/html" title="Hello, GSoC 2026"/><published>2026-05-14T00:00:00+00:00</published><updated>2026-05-14T00:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Ashwani_Kumar/blog/2026/hello-gsoc</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Ashwani_Kumar/blog/2026/hello-gsoc/"><![CDATA[<p>Welcome! This is the first post on my GSoC 2026 blog.</p> <p>I’ll be contributing to <a href="https://github.com/JdeRobot/RoboticsAcademy">RoboticsAcademy</a> — JdeRobot’s open-source framework for learning robotics, computer vision, and AI through interactive exercises.</p> <p>Over the coming weeks I’ll use this space for:</p> <ul> <li><strong>Weekly updates</strong> — what I worked on, what I learned, what’s blocked.</li> <li><strong>Design notes</strong> — the reasoning behind non-obvious decisions.</li> <li><strong>Demos</strong> — short videos or screenshots as features come together.</li> </ul> <p>More details about the project scope and timeline coming in the next post.</p>]]></content><author><name></name></author><category term="updates"/><category term="gsoc"/><category term="intro"/><summary type="html"><![CDATA[First post — kicking off my Google Summer of Code 2026 project on RoboticsAcademy @ JdeRobot.]]></summary></entry></feed>